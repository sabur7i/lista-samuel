using namespace std;
#include <iostream>

int main()
{
    float matriz[3][3];
    float* ptr = &matriz[0][0];
    
    for(int i = 0;i < 3; i++){
        for(int j = 0; j < 3; j ++){
            cout<<"endereço do elemento:"<<i<<j<<"e"<<(ptr + i * 3 + j);
        }
    }
}
